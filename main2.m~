alpha = 1.6;
grid_size = 20;

%P = compute_distrib(alpha,grid_size);

k= 1:15;
Pos = ones(
ct = zeros(length(k),1);
for i = 1:length(k)
    ct(i) = CoveringTime2(grid_size,alpha,Pos(1:i,:));
end
hold on;
plot(k,ct,'r-');